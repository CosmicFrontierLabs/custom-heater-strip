[package]
name = "engine"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
usvg = "0.45"

shared = { path = "../shared" }
cavalier_contours = "0.7.0"
dxf = "0.6"
